download Fake.csv, True.csv, app.py, lr_model.jb, vectorizer.jb and requirements.txt
then download all requirements by running pip install -r requirements.txt
then, run the app.py file by running code streamlit run app.py
